{
  "name": "paginapeliculas",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "cd ./frontend && npm run build && rm -rf ../backend/dist && cp -r ./dist ../backend/dist && cd ../backend && npm run dev",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC"
}
